<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" type="text/css" />
    <link rel="stylesheet" href="/vendor/border-box.css" type="text/css" />
    <link rel="stylesheet" href="/styles/layout.css" type="text/css">
    <link rel="stylesheet" href="/styles/kitchen.css" type="text/css" />
    <script
      src="https://kit.fontawesome.com/93be37909b.js"
      crossorigin="anonymous"
    ></script>
    <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
    <script type="text/javascript" src="/scripts/app.js"></script>
    <title>Burger Bar || Kitchen</title>
  </head>

  <body>
    <header><%- include('./partials/_navbar'); %></header>

    <main>
      <div class="page-container">
        <% for (order in orders) { %>
          <section>
            <p>Order Id: <%= order %></p>
            <p>Created at: <%= orders[order].created_at %></p>
            <p>Name: <%= orders[order].first_name + ' ' + orders[order].surname %></p>
            <p>Phone: <%= orders[order].phone %></p>
            <p>Order:
              <% for (item of Object.values(orders[order].order_items)) { %>
                <p><%= item.menu_item_name %>: x<%= item.quantity %></p>
              <% } %>
            </p>
            <button>Ready for Pickup</button>
            <button>Completed</button>
          </section>
        <% } %>
      </div>
    </main>
    <footer><%- include('./partials/_footer'); %></footer>
  </body>
</html>
